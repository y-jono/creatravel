import { spotOrMovement } from 'creatravel/helpers/spot-or-movement';
import { module, test } from 'qunit';

module('Unit | Helper | spot or movement');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = spotOrMovement([42]);
  assert.ok(result);
});
